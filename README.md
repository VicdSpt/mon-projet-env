# Mon Application Environnement

## Installation

1. Clone ce repository
2. Installe les dépendances : `npm install`
3. Copie `.env.sample` vers `.env` : `cp .env.sample .env`
4. Modifie `.env` avec tes informations personnelles
5. Lance l'application : `node index.js`
